<!DOCTYPE html>
<html lang="ru">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Italiano — Lezione 4</title>
<link href="https://fonts.googleapis.com/css2?family=Playfair+Display:ital,wght@0,400;0,700;1,400&family=Nunito:wght@300;400;600;700&display=swap" rel="stylesheet">
<style>
  :root {
    --cream: #fdf6ec;
    --terracotta: #c0533a;
    --gold: #d4a843;
    --olive: #5a6e3c;
    --ink: #2c2118;
    --sand: #e8d9c0;
    --mist: #f5ede0;
    --shadow: rgba(44,33,24,0.15);
  }
  * { box-sizing: border-box; margin: 0; padding: 0; }
  body {
    font-family: 'Nunito', sans-serif;
    background: var(--cream);
    color: var(--ink);
    min-height: 100vh;
    overflow-x: hidden;
  }
  body::before {
    content: '';
    position: fixed; inset: 0;
    background:
      radial-gradient(ellipse 60% 40% at 10% 20%, rgba(192,83,58,0.07) 0%, transparent 60%),
      radial-gradient(ellipse 50% 50% at 90% 80%, rgba(212,168,67,0.08) 0%, transparent 60%);
    pointer-events: none; z-index: 0;
  }
  .wrapper { position: relative; z-index: 1; max-width: 800px; margin: 0 auto; padding: 24px 16px 60px; }

  header { text-align: center; margin-bottom: 36px; }
  .flag { font-size: 2.4rem; margin-bottom: 8px; }
  header h1 { font-family: 'Playfair Display', serif; font-size: clamp(2rem,6vw,3rem); color: var(--terracotta); line-height: 1.1; }
  header p { color: #8a7060; font-size: 0.95rem; margin-top: 6px; }

  .tabs { display: flex; gap: 6px; justify-content: center; margin-bottom: 32px; flex-wrap: wrap; }
  .tab-btn {
    padding: 9px 18px; border: 2px solid var(--sand); background: white; border-radius: 50px;
    font-family: 'Nunito', sans-serif; font-size: 0.83rem; font-weight: 700; color: #8a7060;
    cursor: pointer; transition: all 0.2s; letter-spacing: 0.03em;
  }
  .tab-btn:hover { border-color: var(--terracotta); color: var(--terracotta); }
  .tab-btn.active { background: var(--terracotta); border-color: var(--terracotta); color: white; box-shadow: 0 4px 14px rgba(192,83,58,0.3); }

  #section-cards { display: block; }
  #section-match, #section-quiz, #section-fill, #section-piacere { display: none; }

  .category-label {
    font-family: 'Playfair Display', serif; font-size: 0.8rem; font-style: italic;
    color: var(--gold); letter-spacing: 0.1em; text-transform: uppercase;
    margin-bottom: 16px; margin-top: 28px; display: flex; align-items: center; gap: 8px;
  }
  .category-label::after { content:''; flex:1; height:1px; background:var(--sand); }

  .cards-grid { display: grid; grid-template-columns: repeat(auto-fill, minmax(200px, 1fr)); gap: 14px; }

  .card {
    background: white; border-radius: 14px; padding: 20px 16px; cursor: pointer;
    box-shadow: 0 2px 10px var(--shadow); border: 1.5px solid var(--sand);
    transition: transform 0.15s, box-shadow 0.15s; position: relative;
    min-height: 90px; display: flex; flex-direction: column; align-items: flex-start;
    justify-content: center; user-select: none;
  }
  .card:hover { transform: translateY(-3px); box-shadow: 0 6px 20px var(--shadow); }
  .card .it { font-family: 'Playfair Display', serif; font-size: 1.05rem; color: var(--terracotta); }
  .card .ru { font-size: 0.85rem; color: var(--olive); margin-top: 6px; opacity: 0; transition: opacity 0.3s; }
  .card.flipped .ru { opacity: 1; }
  .card .hint { position: absolute; top: 10px; right: 12px; font-size: 0.7rem; color: #c8b8a0; font-style: italic; }
  .card.flipped .hint { display: none; }

  .game-header { text-align: center; margin-bottom: 24px; }
  .game-header h2 { font-family: 'Playfair Display', serif; font-size: 1.6rem; color: var(--terracotta); }
  .game-header p { color: #8a7060; font-size: 0.88rem; margin-top: 4px; }

  .match-board { display: grid; grid-template-columns: 1fr 1fr; gap: 10px; margin-bottom: 20px; }
  .match-col { display: flex; flex-direction: column; gap: 8px; }
  .match-item {
    background: white; border: 2px solid var(--sand); border-radius: 10px; padding: 12px 14px;
    cursor: pointer; font-size: 0.88rem; text-align: center; transition: all 0.2s;
    min-height: 46px; display: flex; align-items: center; justify-content: center; line-height: 1.3;
  }
  .match-item.it-word { font-family: 'Playfair Display', serif; color: var(--terracotta); font-size: 0.95rem; }
  .match-item.ru-word { color: var(--olive); }
  .match-item:hover:not(.matched):not(.wrong) { border-color: var(--gold); background: var(--mist); }
  .match-item.selected { border-color: var(--terracotta); background: rgba(192,83,58,0.08); }
  .match-item.matched { border-color: var(--olive); background: rgba(90,110,60,0.1); opacity: 0.6; cursor: default; }
  .match-item.wrong { border-color: #e05050; background: rgba(224,80,80,0.08); animation: shake 0.4s; }
  @keyframes shake { 0%,100%{transform:translateX(0)} 25%{transform:translateX(-6px)} 75%{transform:translateX(6px)} }

  .score-bar {
    display: flex; align-items: center; justify-content: space-between;
    background: white; border-radius: 12px; padding: 12px 18px;
    border: 1.5px solid var(--sand); margin-bottom: 16px;
  }
  .score-num { font-family: 'Playfair Display', serif; font-size: 1.6rem; color: var(--terracotta); }
  .score-label { font-size: 0.78rem; color: #8a7060; text-transform: uppercase; letter-spacing: 0.06em; }

  .btn {
    display: inline-block; padding: 11px 28px; background: var(--terracotta); color: white;
    border: none; border-radius: 50px; font-family: 'Nunito', sans-serif; font-size: 0.9rem;
    font-weight: 700; cursor: pointer; transition: all 0.2s; box-shadow: 0 4px 14px rgba(192,83,58,0.25);
  }
  .btn:hover { background: #a8422b; transform: translateY(-1px); }
  .btn.secondary { background: white; color: var(--terracotta); border: 2px solid var(--terracotta); box-shadow: none; }
  .btn-row { display: flex; gap: 10px; justify-content: center; flex-wrap: wrap; margin-top: 16px; }

  .quiz-progress { height: 6px; background: var(--sand); border-radius: 99px; margin-bottom: 28px; overflow: hidden; }
  .quiz-progress-fill { height: 100%; background: linear-gradient(90deg, var(--terracotta), var(--gold)); border-radius: 99px; transition: width 0.4s; }
  .quiz-card { background: white; border-radius: 18px; padding: 32px 24px; text-align: center; box-shadow: 0 4px 20px var(--shadow); border: 1.5px solid var(--sand); margin-bottom: 20px; }
  .quiz-question-label { font-size: 0.78rem; text-transform: uppercase; letter-spacing: 0.1em; color: #b0a090; margin-bottom: 12px; }
  .quiz-question { font-family: 'Playfair Display', serif; font-size: clamp(1.3rem,4vw,1.9rem); color: var(--ink); margin-bottom: 6px; }
  .quiz-sub { font-size: 0.8rem; color: #8a7060; font-style: italic; }
  .quiz-options { display: grid; grid-template-columns: 1fr 1fr; gap: 10px; margin-top: 8px; }
  .quiz-option {
    padding: 13px 10px; background: white; border: 2px solid var(--sand); border-radius: 12px;
    font-size: 0.9rem; cursor: pointer; transition: all 0.2s; font-family: 'Nunito', sans-serif; color: var(--ink);
  }
  .quiz-option:hover:not(.correct):not(.wrong) { border-color: var(--gold); background: var(--mist); }
  .quiz-option.correct { border-color: var(--olive); background: rgba(90,110,60,0.12); color: var(--olive); font-weight: 700; }
  .quiz-option.wrong { border-color: #e05050; background: rgba(224,80,80,0.08); color: #c03030; }
  .quiz-feedback { text-align: center; font-size: 0.9rem; margin-top: 10px; min-height: 24px; font-weight: 600; }
  .quiz-feedback.good { color: var(--olive); }
  .quiz-feedback.bad { color: #c03030; }
  .quiz-counter { font-size: 0.85rem; color: #8a7060; text-align: center; margin-bottom: 10px; }
  .progress-dots { display: flex; gap: 6px; justify-content: center; margin-bottom: 20px; flex-wrap: wrap; }
  .dot { width: 10px; height: 10px; border-radius: 50%; background: var(--sand); transition: all 0.3s; }
  .dot.done { background: var(--olive); }
  .dot.active { background: var(--terracotta); transform: scale(1.3); }

  .fill-card { background: white; border-radius: 18px; padding: 28px 22px; box-shadow: 0 4px 20px var(--shadow); border: 1.5px solid var(--sand); margin-bottom: 20px; }
  .fill-prompt { font-size: 0.8rem; color: #8a7060; text-transform: uppercase; letter-spacing: 0.08em; margin-bottom: 10px; }
  .fill-situation { font-family: 'Playfair Display', serif; font-size: 1.1rem; color: var(--ink); line-height: 1.7; margin-bottom: 16px; font-style: italic; }
  .fill-input-row { display: flex; gap: 10px; align-items: center; flex-wrap: wrap; }
  .fill-input {
    flex: 1; min-width: 160px; padding: 11px 16px; border: 2px solid var(--sand); border-radius: 10px;
    font-family: 'Playfair Display', serif; font-size: 1rem; color: var(--ink);
    outline: none; transition: border-color 0.2s; background: var(--mist);
  }
  .fill-input:focus { border-color: var(--terracotta); background: white; }
  .fill-input.correct { border-color: var(--olive); background: rgba(90,110,60,0.08); }
  .fill-input.wrong { border-color: #e05050; }
  .fill-feedback { font-size: 0.88rem; margin-top: 10px; min-height: 20px; font-weight: 600; }
  .fill-feedback.good { color: var(--olive); }
  .fill-feedback.bad { color: #c03030; }

  .result-screen { text-align: center; padding: 40px 20px; }
  .result-emoji { font-size: 3.5rem; margin-bottom: 12px; }
  .result-title { font-family: 'Playfair Display', serif; font-size: 2rem; color: var(--terracotta); margin-bottom: 8px; }
  .result-sub { color: #8a7060; margin-bottom: 24px; }
  .result-score { font-family: 'Playfair Display', serif; font-size: 3rem; color: var(--gold); }
  .hidden { display: none !important; }

  /* PIACERE GAME */
  .piacere-intro {
    background: white; border-radius: 14px; padding: 14px 18px; border: 1.5px solid var(--sand);
    margin-bottom: 16px; text-align: center; color: #8a7060; font-size: 0.88rem; line-height: 1.6;
  }
  .piacere-intro strong { color: var(--terracotta); }
  .piacere-prompt {
    background: white; border-radius: 18px; padding: 22px;
    border: 1.5px solid var(--sand); text-align: center; margin-bottom: 16px;
  }
  .piacere-q-label { font-size: 0.75rem; color: #b0a090; text-transform: uppercase; letter-spacing: 0.1em; margin-bottom: 8px; }
  .piacere-subject { font-family: 'Playfair Display', serif; font-size: 1.6rem; color: var(--terracotta); font-style: italic; }
  .piacere-options { display: grid; grid-template-columns: 1fr 1fr; gap: 10px; }
  .piacere-option {
    padding: 14px 10px; background: white; border: 2px solid var(--sand); border-radius: 12px;
    cursor: pointer; transition: all 0.2s; font-family: 'Playfair Display', serif;
    font-size: 1rem; color: var(--ink); text-align: center; line-height: 1.3;
  }
  .piacere-option:hover { border-color: var(--gold); background: var(--mist); }
  .piacere-option.correct { border-color: var(--olive); background: rgba(90,110,60,0.12); color: var(--olive); }
  .piacere-option.wrong { border-color: #e05050; background: rgba(224,80,80,0.08); color: #c03030; }
</style>
</head>
<body>
<div class="wrapper">
  <header>
    <div class="flag">🎭</div>
    <h1>Lezione 4</h1>
    <p>Tempo libero — свободное время, хобби, спорт, музыка</p>
  </header>

  <div class="tabs">
    <button class="tab-btn active" onclick="showSection('cards')">📚 Карточки</button>
    <button class="tab-btn" onclick="showSection('match')">🔗 Соединить</button>
    <button class="tab-btn" onclick="showSection('piacere')">❤️ Mi piace?</button>
    <button class="tab-btn" onclick="showSection('quiz')">🎯 Тест</button>
    <button class="tab-btn" onclick="showSection('fill')">✏️ Вписать</button>
  </div>

  <!-- CARDS -->
  <div id="section-cards">
    <div class="category-label">Хобби и активности</div>
    <div class="cards-grid" id="grid-hobby"></div>

    <div class="category-label">Спорт</div>
    <div class="cards-grid" id="grid-sport"></div>

    <div class="category-label">Музыка и искусство</div>
    <div class="cards-grid" id="grid-music"></div>

    <div class="category-label">Mi piace / Mi piacciono — вкусы и предпочтения</div>
    <div class="cards-grid" id="grid-piace"></div>

    <div class="category-label">Частота — как часто?</div>
    <div class="cards-grid" id="grid-freq"></div>

    <div class="category-label">Дни недели и время</div>
    <div class="cards-grid" id="grid-time"></div>

    <div class="category-label">Полезные выражения</div>
    <div class="cards-grid" id="grid-useful"></div>

    <div class="category-label">Работа, доставка, документы</div>
    <div class="cards-grid" id="grid-work"></div>

    <div class="category-label">Шутки и юмор</div>
    <div class="cards-grid" id="grid-jokes"></div>

    <p style="text-align:center;color:#b0a090;font-size:0.8rem;margin-top:20px;font-style:italic;">Нажмите на карточку, чтобы увидеть перевод</p>
  </div>

  <!-- MATCH -->
  <div id="section-match">
    <div class="game-header">
      <h2>Соедини слова</h2>
      <p>Выбери итальянское слово, затем его перевод</p>
    </div>
    <div class="score-bar">
      <div><div class="score-num" id="match-score">0</div><div class="score-label">Совпадений</div></div>
      <div><div class="score-num" id="match-total">0</div><div class="score-label">Всего пар</div></div>
      <div><div class="score-num" id="match-errors">0</div><div class="score-label">Ошибок</div></div>
    </div>
    <div class="match-board">
      <div class="match-col" id="match-left"></div>
      <div class="match-col" id="match-right"></div>
    </div>
    <div class="btn-row"><button class="btn" onclick="initMatch()">🔄 Новый раунд</button></div>
    <div id="match-result" class="result-screen hidden">
      <div class="result-emoji">🎉</div>
      <div class="result-title">Bravissimo!</div>
      <div class="result-sub">Все пары найдены!</div>
      <div class="btn-row"><button class="btn" onclick="initMatch()">Ещё раз</button></div>
    </div>
  </div>

  <!-- PIACERE GAME -->
  <div id="section-piacere">
    <div class="game-header">
      <h2>Mi piace o mi piacciono?</h2>
      <p>Выбери правильную форму глагола piacere</p>
    </div>
    <div class="piacere-intro">
      <strong>Правило:</strong> <em>mi piace</em> + единственное число / инфинитив &nbsp;·&nbsp; <em>mi piacciono</em> + множественное число<br>
      <em>Mi piace il calcio. &nbsp; Mi piacciono i film. &nbsp; Mi piace leggere.</em>
    </div>
    <div class="score-bar">
      <div><div class="score-num" id="pc-score">0</div><div class="score-label">Правильно</div></div>
      <div><div class="score-num" id="pc-current">1</div><div class="score-label">Из 12</div></div>
      <div><div class="score-num" id="pc-errors">0</div><div class="score-label">Ошибок</div></div>
    </div>
    <div id="piacere-play">
      <div class="piacere-prompt">
        <div class="piacere-q-label">Come si dice?</div>
        <div class="piacere-subject" id="pc-subject">—</div>
      </div>
      <div class="piacere-options" id="pc-options"></div>
      <div class="quiz-feedback" id="pc-feedback"></div>
      <div class="btn-row"><button class="btn secondary hidden" id="pc-next" onclick="nextPiacere()">Дальше →</button></div>
    </div>
    <div id="piacere-result" class="result-screen hidden">
      <div class="result-emoji" id="pc-emoji">🏆</div>
      <div class="result-title">Risultato</div>
      <div class="result-score" id="pc-final"></div>
      <div class="result-sub" id="pc-sub"></div>
      <div class="btn-row"><button class="btn" onclick="initPiacere()">Заново</button></div>
    </div>
  </div>

  <!-- QUIZ -->
  <div id="section-quiz">
    <div class="game-header">
      <h2>Тест с выбором</h2>
      <p>Выбери правильный перевод</p>
    </div>
    <div class="quiz-progress"><div class="quiz-progress-fill" id="quiz-bar"></div></div>
    <div class="progress-dots" id="quiz-dots"></div>
    <div class="quiz-counter" id="quiz-counter"></div>
    <div id="quiz-play">
      <div class="quiz-card">
        <div class="quiz-question-label" id="quiz-dir">Итальянский → Русский</div>
        <div class="quiz-question" id="quiz-word">—</div>
        <div class="quiz-sub" id="quiz-ctx"></div>
      </div>
      <div class="quiz-options" id="quiz-options"></div>
      <div class="quiz-feedback" id="quiz-feedback"></div>
      <div class="btn-row"><button class="btn secondary" id="quiz-next-btn" onclick="nextQuiz()" style="display:none">Дальше →</button></div>
    </div>
    <div id="quiz-result" class="result-screen hidden">
      <div class="result-emoji" id="quiz-emoji">🏆</div>
      <div class="result-title">Risultato</div>
      <div class="result-score" id="quiz-final-score"></div>
      <div class="result-sub" id="quiz-final-sub"></div>
      <div class="btn-row">
        <button class="btn" onclick="initQuiz()">Заново</button>
        <button class="btn secondary" onclick="initQuiz(true)">Только ошибки</button>
      </div>
    </div>
  </div>

  <!-- FILL -->
  <div id="section-fill">
    <div class="game-header">
      <h2>Вспомни по-итальянски</h2>
      <p>Прочитай ситуацию и введи нужное слово или фразу</p>
    </div>
    <div class="quiz-progress"><div class="quiz-progress-fill" id="fill-bar"></div></div>
    <div class="quiz-counter" id="fill-counter"></div>
    <div id="fill-play">
      <div class="fill-card">
        <div class="fill-prompt">Как сказать по-итальянски?</div>
        <div class="fill-situation" id="fill-situation"></div>
        <div class="fill-input-row">
          <input class="fill-input" id="fill-input" placeholder="Введите ответ..." onkeydown="if(event.key==='Enter')checkFill()">
          <button class="btn" onclick="checkFill()">→</button>
        </div>
        <div class="fill-feedback" id="fill-feedback"></div>
      </div>
      <div class="btn-row">
        <button class="btn secondary" id="fill-skip" onclick="skipFill()">Пропустить</button>
        <button class="btn secondary hidden" id="fill-next" onclick="nextFill()">Следующий →</button>
      </div>
    </div>
    <div id="fill-result" class="result-screen hidden">
      <div class="result-emoji" id="fill-emoji">🌟</div>
      <div class="result-title">Готово!</div>
      <div class="result-score" id="fill-final-score"></div>
      <div class="result-sub" id="fill-final-sub"></div>
      <div class="btn-row"><button class="btn" onclick="initFill()">Заново</button></div>
    </div>
  </div>
</div>

<script>
const vocab = [
  // HOBBY
  { it: "tempo libero", ru: "свободное время", cat: "hobby" },
  { it: "attività", ru: "занятие / активность", cat: "hobby" },
  { it: "giocare", ru: "играть", cat: "hobby" },
  { it: "leggere", ru: "читать", cat: "hobby" },
  { it: "ballare", ru: "танцевать", cat: "hobby" },
  { it: "dipingere", ru: "рисовать / писать красками", cat: "hobby" },
  { it: "guardare", ru: "смотреть", cat: "hobby" },
  { it: "chattare", ru: "переписываться в чате", cat: "hobby" },
  { it: "usare", ru: "использовать", cat: "hobby" },
  { it: "andare al cinema", ru: "идти в кино", cat: "hobby" },
  { it: "lavorare in giardino", ru: "работать в саду", cat: "hobby" },
  { it: "fare la spesa", ru: "делать покупки (продукты)", cat: "hobby" },
  { it: "ascoltare la musica", ru: "слушать музыку", cat: "hobby" },
  { it: "fare una passeggiata", ru: "гулять / совершать прогулку", cat: "hobby" },
  { it: "gioco da tavolo", ru: "настольная игра", cat: "hobby" },
  { it: "giocare a carte", ru: "играть в карты", cat: "hobby" },
  { it: "guardare le notizie", ru: "смотреть новости", cat: "hobby" },
  { it: "fare shopping", ru: "ходить по магазинам", cat: "hobby" },
  { it: "imparare", ru: "учиться / изучать", cat: "hobby" },
  { it: "visitare", ru: "посещать", cat: "hobby" },
  { it: "viaggiare", ru: "путешествовать", cat: "hobby" },
  { it: "andare a teatro", ru: "идти в театр", cat: "hobby" },
  { it: "mangiare fuori", ru: "есть вне дома / ходить в ресторан", cat: "hobby" },
  { it: "stare sui social", ru: "сидеть в соцсетях", cat: "hobby" },
  { it: "escursione", ru: "экскурсия / поход", cat: "hobby" },

  // SPORT
  { it: "fare sport", ru: "заниматься спортом", cat: "sport" },
  { it: "sport", ru: "спорт", cat: "sport" },
  { it: "che sport fai?", ru: "каким спортом ты занимаешься?", cat: "sport" },
  { it: "calcio", ru: "футбол", cat: "sport" },
  { it: "tennis", ru: "теннис", cat: "sport" },
  { it: "correre", ru: "бегать", cat: "sport" },
  { it: "andare in bicicletta", ru: "ездить на велосипеде", cat: "sport" },
  { it: "fare yoga", ru: "заниматься йогой", cat: "sport" },
  { it: "piscina", ru: "бассейн", cat: "sport" },
  { it: "ballo", ru: "танец / танцы", cat: "sport" },
  { it: "sudamericano", ru: "южноамериканский", cat: "sport" },

  // MUSIC & ART
  { it: "suonare", ru: "играть (на инструменте)", cat: "music" },
  { it: "strumento", ru: "инструмент", cat: "music" },
  { it: "strumento musicale", ru: "музыкальный инструмент", cat: "music" },
  { it: "chitarra", ru: "гитара", cat: "music" },
  { it: "pianoforte", ru: "пианино / фортепиано", cat: "music" },
  { it: "basso", ru: "бас-гитара", cat: "music" },
  { it: "canzone", ru: "песня", cat: "music" },
  { it: "opera", ru: "опера", cat: "music" },
  { it: "tango", ru: "танго", cat: "music" },
  { it: "musica rock", ru: "рок-музыка", cat: "music" },
  { it: "musica classica", ru: "классическая музыка", cat: "music" },
  { it: "arte", ru: "искусство", cat: "music" },
  { it: "serie tv", ru: "сериал", cat: "music" },
  { it: "fumetto", ru: "комикс", cat: "music" },
  { it: "fantascienza", ru: "фантастика", cat: "music" },
  { it: "moderno", ru: "современный", cat: "music" },
  { it: "lingua italiana", ru: "итальянский язык", cat: "music" },

  // PIACE
  { it: "mi piace...", ru: "мне нравится... (+ ед.ч. / инфинитив)", cat: "piace" },
  { it: "mi piacciono...", ru: "мне нравятся... (+ мн.ч.)", cat: "piace" },
  { it: "ti piace...?", ru: "тебе нравится...?", cat: "piace" },
  { it: "ti piacciono...?", ru: "тебе нравятся...?", cat: "piace" },
  { it: "anche a me", ru: "мне тоже (нравится)", cat: "piace" },
  { it: "neanche a me", ru: "мне тоже нет (не нравится)", cat: "piace" },
  { it: "anche io", ru: "я тоже", cat: "piace" },
  { it: "a me", ru: "мне (лично)", cat: "piace" },
  { it: "odiare", ru: "ненавидеть", cat: "piace" },
  { it: "veramente", ru: "действительно / по-настоящему", cat: "piace" },

  // FREQUENCY
  { it: "di solito", ru: "обычно", cat: "freq" },
  { it: "sempre", ru: "всегда", cat: "freq" },
  { it: "spesso", ru: "часто", cat: "freq" },
  { it: "qualche volta", ru: "иногда", cat: "freq" },
  { it: "quasi sempre", ru: "почти всегда", cat: "freq" },
  { it: "non... mai", ru: "никогда", cat: "freq" },
  { it: "tantissimo", ru: "очень много / очень сильно", cat: "freq" },
  { it: "soprattutto", ru: "прежде всего / особенно", cat: "freq" },
  { it: "nuovo", ru: "новый", cat: "freq" },

  // TIME
  { it: "fine settimana", ru: "выходные", cat: "time" },
  { it: "settimana", ru: "неделя", cat: "time" },
  { it: "mercoledì", ru: "среда", cat: "time" },
  { it: "giovedì", ru: "четверг", cat: "time" },
  { it: "venerdì", ru: "пятница", cat: "time" },
  { it: "domani", ru: "завтра", cat: "time" },
  { it: "da due anni", ru: "уже два года / в течение двух лет", cat: "time" },
  { it: "che giorno è oggi?", ru: "какой сегодня день?", cat: "time" },
  { it: "che ora è?", ru: "который час? (ед.ч.)", cat: "time" },
  { it: "che ore sono?", ru: "который час? (мн.ч.)", cat: "time" },
  { it: "è l'una", ru: "сейчас один час", cat: "time" },
  { it: "sono le... e trenta / e mezza", ru: "...и тридцать / полпервого", cat: "time" },
  { it: "sono le... meno venti / meno un quarto", ru: "...без двадцати / без четверти", cat: "time" },
  { it: "sono le... e un quarto / e quindici", ru: "...и четверть / и пятнадцать", cat: "time" },
  { it: "mezzanotte", ru: "полночь", cat: "time" },

  // USEFUL
  { it: "che cosa fai nel tempo libero?", ru: "что ты делаешь в свободное время?", cat: "useful" },
  { it: "a casa", ru: "дома", cat: "useful" },
  { it: "uscire", ru: "выходить (из дома)", cat: "useful" },
  { it: "andare", ru: "идти / ехать", cat: "useful" },
  { it: "discoteca", ru: "дискотека / ночной клуб", cat: "useful" },
  { it: "il mio ragazzo", ru: "мой парень / мой молодой человек", cat: "useful" },
  { it: "come te", ru: "как ты / так же, как ты", cat: "useful" },
  { it: "ma come?", ru: "как же так?", cat: "useful" },
  { it: "brasiliano", ru: "бразильский / бразилец", cat: "useful" },
  { it: "argentino", ru: "аргентинский / аргентинец", cat: "useful" },
  { it: "economia", ru: "экономика", cat: "useful" },
  { it: "albergo", ru: "гостиница / отель", cat: "useful" },
  { it: "corso", ru: "курс (занятий)", cat: "useful" },
  { it: "perché", ru: "почему / потому что", cat: "useful" },
  { it: "tutto", ru: "всё / весь", cat: "useful" },
  { it: "mare", ru: "море", cat: "useful" },
  { it: "giusto", ru: "правильно / верно", cat: "useful" },
  { it: "sbagliato", ru: "неправильно / неверно", cat: "useful" },
  { it: "oddio!", ru: "боже мой! / ой!", cat: "useful" },
  { it: "interessante", ru: "интересный", cat: "useful" },
  { it: "sapere", ru: "знать", cat: "useful" },

  // WORK & DELIVERY
  { it: "ditta", ru: "фирма / компания", cat: "work" },
  { it: "merce", ru: "товар", cat: "work" },
  { it: "spedire", ru: "отправлять (посылку, груз)", cat: "work" },
  { it: "inviare", ru: "отправлять (документ, сообщение)", cat: "work" },
  { it: "mandare", ru: "посылать / отправлять (общее)", cat: "work" },
  { it: "partita", ru: "матч / партия игры; партия товара", cat: "work" },
  { it: "porta", ru: "дверь", cat: "work" },
  { it: "bussare", ru: "стучать (в дверь)", cat: "work" },
  { it: "aggiungere", ru: "добавлять", cat: "work" },
  { it: "spiegare", ru: "объяснять", cat: "work" },

  // JOKES & HUMOUR
  { it: "barzelletta", ru: "анекдот / шутка-история", cat: "jokes" },
  { it: "scherzo", ru: "шутка / розыгрыш", cat: "jokes" },
  { it: "battuta", ru: "острота / шутливая реплика", cat: "jokes" },
  { it: "pancetta", ru: "бекон / панчетта (тоже: животик)", cat: "jokes" },
  { it: "ho caldo", ru: "мне жарко", cat: "jokes" },
  { it: "sono caldo", ru: "я горячий / возбуждён (осторожно — не «мне жарко»!)", cat: "jokes" },
];

const catMap = {
  hobby: "grid-hobby",
  sport: "grid-sport",
  music: "grid-music",
  piace: "grid-piace",
  freq: "grid-freq",
  time: "grid-time",
  useful: "grid-useful",
  work: "grid-work",
  jokes: "grid-jokes"
};

function renderCards() {
  Object.values(catMap).forEach(id => document.getElementById(id).innerHTML = '');
  vocab.forEach(v => {
    const el = document.createElement('div');
    el.className = 'card';
    el.innerHTML = `<span class="hint">нажми</span><div class="it">${v.it}</div><div class="ru">${v.ru}</div>`;
    el.onclick = () => el.classList.toggle('flipped');
    const grid = document.getElementById(catMap[v.cat]);
    if (grid) grid.appendChild(el);
  });
}

function showSection(name) {
  ['cards','match','piacere','quiz','fill'].forEach(s => {
    document.getElementById('section-'+s).style.display = s===name ? 'block' : 'none';
  });
  document.querySelectorAll('.tab-btn').forEach((b,i) => {
    b.classList.toggle('active', ['cards','match','piacere','quiz','fill'][i]===name);
  });
  if (name==='match') initMatch();
  if (name==='piacere') initPiacere();
  if (name==='quiz') initQuiz();
  if (name==='fill') initFill();
}

function shuffle(arr) { return [...arr].sort(()=>Math.random()-0.5); }

// ===================== MATCH =====================
let matchSelected = null, matchScore = 0, matchErrors = 0;

function initMatch() {
  const pool = shuffle(vocab).slice(0,8);
  matchScore = 0; matchErrors = 0;
  document.getElementById('match-score').textContent = 0;
  document.getElementById('match-errors').textContent = 0;
  document.getElementById('match-total').textContent = pool.length;
  document.getElementById('match-result').classList.add('hidden');
  matchSelected = null;
  const leftEl = document.getElementById('match-left');
  const rightEl = document.getElementById('match-right');
  leftEl.innerHTML = ''; rightEl.innerHTML = '';
  shuffle(pool).forEach(v => {
    const el = document.createElement('div');
    el.className = 'match-item it-word';
    el.textContent = v.it; el.dataset.key = v.it; el.dataset.side = 'it';
    el.onclick = () => handleMatchClick(el);
    leftEl.appendChild(el);
  });
  shuffle(pool).forEach(v => {
    const el = document.createElement('div');
    el.className = 'match-item ru-word';
    el.textContent = v.ru; el.dataset.key = v.it; el.dataset.side = 'ru';
    el.onclick = () => handleMatchClick(el);
    rightEl.appendChild(el);
  });
}

function handleMatchClick(el) {
  if (el.classList.contains('matched') || el.classList.contains('wrong')) return;
  if (!matchSelected) {
    document.querySelectorAll('.match-item').forEach(e => e.classList.remove('selected'));
    el.classList.add('selected'); matchSelected = el; return;
  }
  if (el === matchSelected) { el.classList.remove('selected'); matchSelected = null; return; }
  if (el.dataset.side === matchSelected.dataset.side) {
    document.querySelectorAll('.match-item').forEach(e => e.classList.remove('selected'));
    el.classList.add('selected'); matchSelected = el; return;
  }
  if (el.dataset.key === matchSelected.dataset.key) {
    el.classList.add('matched'); matchSelected.classList.add('matched');
    el.classList.remove('selected'); matchSelected.classList.remove('selected');
    matchScore++;
    document.getElementById('match-score').textContent = matchScore;
    matchSelected = null;
    if (matchScore === parseInt(document.getElementById('match-total').textContent)) {
      setTimeout(() => document.getElementById('match-result').classList.remove('hidden'), 500);
    }
  } else {
    el.classList.add('wrong'); matchSelected.classList.add('wrong');
    matchErrors++;
    document.getElementById('match-errors').textContent = matchErrors;
    const a = el, b = matchSelected; matchSelected = null;
    setTimeout(() => { a.classList.remove('wrong','selected'); b.classList.remove('wrong','selected'); }, 700);
  }
}

// ===================== PIACERE GAME =====================
// answer: 'piace' or 'piacciono'
const piacereItems = [
  { subject: "il calcio", answer: "piace", hint: "calcio — ед.ч." },
  { subject: "i film", answer: "piacciono", hint: "film — мн.ч." },
  { subject: "leggere", answer: "piace", hint: "инфинитив → piace" },
  { subject: "la musica classica", answer: "piace", hint: "musica — ед.ч." },
  { subject: "le canzoni", answer: "piacciono", hint: "canzoni — мн.ч." },
  { subject: "viaggiare", answer: "piace", hint: "инфинитив → piace" },
  { subject: "gli sport", answer: "piacciono", hint: "sport — мн.ч." },
  { subject: "fare yoga", answer: "piace", hint: "fare + инфинитив → piace" },
  { subject: "le serie tv", answer: "piacciono", hint: "serie — мн.ч." },
  { subject: "il tennis", answer: "piace", hint: "tennis — ед.ч." },
  { subject: "i giochi da tavolo", answer: "piacciono", hint: "giochi — мн.ч." },
  { subject: "ballare", answer: "piace", hint: "инфинитив → piace" },
  { subject: "l'opera", answer: "piace", hint: "opera — ед.ч." },
  { subject: "le escursioni", answer: "piacciono", hint: "escursioni — мн.ч." },
];

let pcItems = [], pcIdx = 0, pcCorrect = 0, pcErrors = 0, pcAnswered = false;

function initPiacere() {
  pcItems = shuffle(piacereItems).slice(0,12);
  pcIdx = 0; pcCorrect = 0; pcErrors = 0;
  document.getElementById('piacere-result').classList.add('hidden');
  document.getElementById('piacere-play').classList.remove('hidden');
  showPiacere();
}

function showPiacere() {
  if (pcIdx >= pcItems.length) { endPiacere(); return; }
  const item = pcItems[pcIdx];
  pcAnswered = false;
  document.getElementById('pc-subject').textContent = item.subject;
  document.getElementById('pc-current').textContent = pcIdx + 1;
  document.getElementById('pc-score').textContent = pcCorrect;
  document.getElementById('pc-errors').textContent = pcErrors;
  document.getElementById('pc-feedback').textContent = '';
  document.getElementById('pc-feedback').className = 'quiz-feedback';
  document.getElementById('pc-next').classList.add('hidden');

  const opts = [
    { label: 'mi piace', val: 'piace' },
    { label: 'mi piacciono', val: 'piacciono' },
  ];
  const optEl = document.getElementById('pc-options');
  optEl.innerHTML = '';
  opts.forEach(o => {
    const btn = document.createElement('button');
    btn.className = 'piacere-option';
    btn.textContent = o.label;
    btn.onclick = () => {
      if (pcAnswered) return; pcAnswered = true;
      if (o.val === item.answer) {
        btn.classList.add('correct'); pcCorrect++;
        document.getElementById('pc-feedback').textContent = `✓ Bravo! (${item.hint})`;
        document.getElementById('pc-feedback').className = 'quiz-feedback good';
      } else {
        btn.classList.add('wrong'); pcErrors++;
        optEl.querySelectorAll('button').forEach(b => { if (b.textContent === (item.answer==='piace'?'mi piace':'mi piacciono')) b.classList.add('correct'); });
        document.getElementById('pc-feedback').textContent = `Правильно: mi ${item.answer} — ${item.hint}`;
        document.getElementById('pc-feedback').className = 'quiz-feedback bad';
      }
      document.getElementById('pc-score').textContent = pcCorrect;
      document.getElementById('pc-errors').textContent = pcErrors;
      document.getElementById('pc-next').classList.remove('hidden');
    };
    optEl.appendChild(btn);
  });
}

function nextPiacere() { pcIdx++; showPiacere(); }

function endPiacere() {
  document.getElementById('piacere-play').classList.add('hidden');
  document.getElementById('piacere-result').classList.remove('hidden');
  const pct = Math.round(pcCorrect/pcItems.length*100);
  document.getElementById('pc-final').textContent = `${pcCorrect}/${pcItems.length}`;
  document.getElementById('pc-sub').textContent = `${pct}% правильных ответов`;
  document.getElementById('pc-emoji').textContent = pct>=80?'🏆':pct>=50?'👍':'📖';
}

// ===================== QUIZ =====================
let quizItems = [], quizIdx = 0, quizCorrect = 0, quizAnswered = false, quizMistakes = [];

function initQuiz(onlyErrors = false) {
  document.getElementById('quiz-result').classList.add('hidden');
  document.getElementById('quiz-play').classList.remove('hidden');
  quizItems = shuffle(onlyErrors && quizMistakes.length ? quizMistakes : vocab).slice(0, onlyErrors ? quizMistakes.length : 15);
  quizMistakes = []; quizIdx = 0; quizCorrect = 0;
  renderDots(); showQuiz();
}

function renderDots() {
  document.getElementById('quiz-dots').innerHTML = quizItems.map((_,i) =>
    `<div class="dot ${i===0?'active':''}" id="dot-${i}"></div>`).join('');
}

function showQuiz() {
  if (quizIdx >= quizItems.length) { endQuiz(); return; }
  const v = quizItems[quizIdx];
  quizAnswered = false;
  const dir = Math.random() > 0.5;
  document.getElementById('quiz-dir').textContent = dir ? 'Итальянский → Русский' : 'Русский → Итальянский';
  document.getElementById('quiz-word').textContent = dir ? v.it : v.ru;
  document.getElementById('quiz-ctx').textContent = '';
  document.getElementById('quiz-feedback').textContent = '';
  document.getElementById('quiz-feedback').className = 'quiz-feedback';
  document.getElementById('quiz-next-btn').style.display = 'none';
  document.getElementById('quiz-counter').textContent = `Вопрос ${quizIdx+1} из ${quizItems.length}`;
  document.getElementById('quiz-bar').style.width = (quizIdx/quizItems.length*100)+'%';
  document.querySelectorAll('.dot').forEach((d,i) => {
    d.className = 'dot' + (i<quizIdx?' done':i===quizIdx?' active':'');
  });
  const wrong = shuffle(vocab.filter(x=>x.it!==v.it)).slice(0,3);
  const opts = shuffle([v, ...wrong]);
  const optEl = document.getElementById('quiz-options');
  optEl.innerHTML = '';
  opts.forEach(o => {
    const btn = document.createElement('button');
    btn.className = 'quiz-option';
    btn.textContent = dir ? o.ru : o.it;
    btn.onclick = () => {
      if (quizAnswered) return; quizAnswered = true;
      const correct = o.it === v.it;
      btn.classList.add(correct ? 'correct' : 'wrong');
      if (!correct) {
        quizMistakes.push(v);
        optEl.querySelectorAll('button').forEach(b => { if(b.textContent===(dir?v.ru:v.it)) b.classList.add('correct'); });
        document.getElementById('quiz-feedback').textContent = `Правильно: ${dir?v.ru:v.it}`;
        document.getElementById('quiz-feedback').className = 'quiz-feedback bad';
      } else {
        quizCorrect++;
        document.getElementById('quiz-feedback').textContent = '✓ Bravo!';
        document.getElementById('quiz-feedback').className = 'quiz-feedback good';
      }
      document.getElementById('quiz-next-btn').style.display = 'inline-block';
    };
    optEl.appendChild(btn);
  });
}

function nextQuiz() { quizIdx++; showQuiz(); }

function endQuiz() {
  document.getElementById('quiz-play').classList.add('hidden');
  document.getElementById('quiz-result').classList.remove('hidden');
  const pct = Math.round(quizCorrect/quizItems.length*100);
  document.getElementById('quiz-final-score').textContent = `${quizCorrect}/${quizItems.length}`;
  document.getElementById('quiz-final-sub').textContent = `${pct}% правильных ответов`;
  document.getElementById('quiz-emoji').textContent = pct>=80?'🏆':pct>=50?'👍':'📖';
}

// ===================== FILL =====================
const fillTasks = [
  { situation: "Что ты делаешь в свободное время? (вопрос):", answer: "che cosa fai nel tempo libero?" },
  { situation: "Каким спортом ты занимаешься? (вопрос):", answer: "che sport fai?" },
  { situation: "Сказать «я занимаюсь спортом»:", answer: "faccio sport" },
  { situation: "Сказать «я хожу в кино»:", answer: "vado al cinema" },
  { situation: "Сказать «я слушаю музыку»:", answer: "ascolto la musica" },
  { situation: "Сказать «я читаю» (инфинитив):", answer: "leggere" },
  { situation: "Сказать «я играю на гитаре»:", answer: "suono la chitarra" },
  { situation: "Сказать «мне нравится танцевать»:", answer: "mi piace ballare" },
  { situation: "Сказать «мне нравятся фильмы»:", answer: "mi piacciono i film" },
  { situation: "Сказать «мне тоже нравится»:", answer: "anche a me" },
  { situation: "Сказать «мне тоже не нравится»:", answer: "neanche a me" },
  { situation: "Сказать «обычно»:", answer: "di solito" },
  { situation: "Сказать «никогда»:", answer: "non... mai" },
  { situation: "Сказать «иногда»:", answer: "qualche volta" },
  { situation: "Сказать «всегда»:", answer: "sempre" },
  { situation: "Спросить «который час?» (мн.ч.):", answer: "che ore sono?" },
  { situation: "Сказать «половина первого»:", answer: "l'una e mezza" },
  { situation: "Спросить «какой сегодня день?»:", answer: "che giorno è oggi?" },
  { situation: "Сказать «особенно / прежде всего»:", answer: "soprattutto" },
  { situation: "Сказать «правильно» и «неправильно»:", answer: "giusto / sbagliato" },
  { situation: "Сказать «выходные»:", answer: "fine settimana" },
  { situation: "Глагол «выходить из дома» (инфинитив):", answer: "uscire" },
  { situation: "Сказать «идти в театр»:", answer: "andare a teatro" },
  { situation: "Сказать «гулять» (фраза с fare):", answer: "fare una passeggiata" },
  { situation: "Сказать «ненавидеть» (инфинитив):", answer: "odiare" },
  { situation: "Глагол «отправлять» посылку или груз:", answer: "spedire" },
  { situation: "Глагол «отправлять» документ или сообщение:", answer: "inviare" },
  { situation: "Глагол «посылать» в общем смысле:", answer: "mandare" },
  { situation: "Сказать «товар»:", answer: "merce" },
  { situation: "Сказать «фирма / компания»:", answer: "ditta" },
  { situation: "Сказать «дверь»:", answer: "porta" },
  { situation: "Глагол «стучать» (в дверь):", answer: "bussare" },
  { situation: "Глагол «добавлять»:", answer: "aggiungere" },
  { situation: "Глагол «объяснять»:", answer: "spiegare" },
  { situation: "Сказать «анекдот» (история-шутка):", answer: "barzelletta" },
  { situation: "Сказать «шутка / розыгрыш»:", answer: "scherzo" },
  { situation: "Сказать «мне жарко»:", answer: "ho caldo" },
  { situation: "Глагол «знать» в инфинитиве:", answer: "sapere" },
];

let fillIdx = 0, fillCorrect = 0, fillItems = [];

function initFill() {
  fillItems = shuffle(fillTasks);
  fillIdx = 0; fillCorrect = 0;
  document.getElementById('fill-result').classList.add('hidden');
  document.getElementById('fill-play').classList.remove('hidden');
  showFill();
}

function showFill() {
  if (fillIdx >= fillItems.length) { endFill(); return; }
  const t = fillItems[fillIdx];
  document.getElementById('fill-situation').textContent = t.situation;
  document.getElementById('fill-input').value = '';
  document.getElementById('fill-input').className = 'fill-input';
  document.getElementById('fill-feedback').textContent = '';
  document.getElementById('fill-feedback').className = 'fill-feedback';
  document.getElementById('fill-counter').textContent = `Задание ${fillIdx+1} из ${fillItems.length}`;
  document.getElementById('fill-bar').style.width = (fillIdx/fillItems.length*100)+'%';
  document.getElementById('fill-skip').classList.remove('hidden');
  document.getElementById('fill-next').classList.add('hidden');
  document.getElementById('fill-input').disabled = false;
  setTimeout(() => document.getElementById('fill-input').focus(), 100);
}

function normalize(s) { return s.toLowerCase().trim().replace(/[!?\.,]/g,''); }

function checkFill() {
  const t = fillItems[fillIdx];
  const val = document.getElementById('fill-input').value;
  if (!val.trim()) return;
  const correct = normalize(val) === normalize(t.answer);
  document.getElementById('fill-input').classList.add(correct ? 'correct' : 'wrong');
  document.getElementById('fill-input').disabled = true;
  if (correct) {
    fillCorrect++;
    document.getElementById('fill-feedback').textContent = '✓ Bravo!';
    document.getElementById('fill-feedback').className = 'fill-feedback good';
  } else {
    document.getElementById('fill-feedback').textContent = `✗ Правильно: ${t.answer}`;
    document.getElementById('fill-feedback').className = 'fill-feedback bad';
  }
  document.getElementById('fill-skip').classList.add('hidden');
  document.getElementById('fill-next').classList.remove('hidden');
}

function skipFill() {
  const t = fillItems[fillIdx];
  document.getElementById('fill-feedback').textContent = `Правильный ответ: ${t.answer}`;
  document.getElementById('fill-feedback').className = 'fill-feedback bad';
  document.getElementById('fill-input').disabled = true;
  document.getElementById('fill-skip').classList.add('hidden');
  document.getElementById('fill-next').classList.remove('hidden');
}

function nextFill() { fillIdx++; showFill(); }

function endFill() {
  document.getElementById('fill-play').classList.add('hidden');
  document.getElementById('fill-result').classList.remove('hidden');
  const pct = Math.round(fillCorrect/fillItems.length*100);
  document.getElementById('fill-final-score').textContent = `${fillCorrect}/${fillItems.length}`;
  document.getElementById('fill-final-sub').textContent = `${pct}% правильных ответов`;
  document.getElementById('fill-emoji').textContent = pct>=80?'🌟':pct>=50?'📝':'🎭';
}

renderCards();
</script>
</body>
</html>
